 version: 2 
 jobs:
   build:
     working_directory: ~/circleci2
     docker:
       - image: tomcat 
     steps:
       - checkout
        command:
           name: Running Test
           echo "S3 texting" > s3file.txt

