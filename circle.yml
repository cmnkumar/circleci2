 version: 2 
 jobs:
   build:
     working_directory: ~/circleci2
     docker:
       - image: buildpack-deps:trusty
     steps:
       - checkout
       - run: echo "S3 texting" > s4file.txt
       - run: apt-get install python-pip
       - run: pip -V
       - run: echo " installing awscli"
       - run: pip install awscli

