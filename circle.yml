 version: 3 
 jobs:
   build:
     working_directory: ~/circleci2
     docker:
       - image: circleci/openjdk:8
        dependencies:
        override:
        - sudo pip install awscli
     steps:
       - checkout
       - run: echo "hello world 123"
       - run: echco "S3 texting" > s3file.txt
