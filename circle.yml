 version: 2 
 jobs:
   build:
     working_directory: ~/circleci2
     docker:
       - image: jugheadstar/myapacheweb:v1 
     steps:
       - checkout
       - run: echo "S3 texting" > s5file.txt
       - run: sudo yum -y update
       - run: sudo yum install python-pip

       - run: pip -V
       - run: echo " installing awscli"
       - run: pip install awscli

