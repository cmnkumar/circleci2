 version: 2 
 jobs:
   build:
     working_directory: ~/circleci2
     docker:
       - image: circleci/openjdk:8
     dependencies:
         override:
        - sudo apt-get install pip
        - sudo pip install awscli
     steps:
       - checkout
       - run: echo "S3 texting" > s3file.txt
       - run: echo "S3 texting again now"
