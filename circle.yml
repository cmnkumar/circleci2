 version: 2 
 jobs:
   build:
     working_directory: ~/circleci2
     docker:
       - image: circleci/openjdk:8
     steps:
        - checkout
        - run:
          command: sudo pip install awscli  
